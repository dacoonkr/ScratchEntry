### 이곳에 적힌 문제들은 제작자가 인지하고 있으며, 추후 업데이트로 해결될 수 있는 항목입니다. 발생한 문제가 이곳에 없다면 Issue를 제기해 주세요.

## 전체를 변환할 수 없는 경우
발견된 사항이 없습니다.

## 일부를 변환할 수 없는 경우
E101. 스크래치에서 지원하는 회전타입(고정, 좌우, 전방향)을 엔트리에서 구현할 수 없습니다.  
~~E102. 스크래치에서 랜덤 위치로 이동하기는 엔트리에서 구현할 수 없습니다.~~  
E103. 오디오 변환에 문제가 있어, 오디오는 옮겨지지 않습니다.  
E104. 스크래치에서 이벤트 구문 중, "마이크 음량"을 사용하는 이벤트는 엔트리에서 구현할 수 없습니다.  
~~E105. 스크래치에서 이벤트 구문 중, "타이머"을 사용하는 이벤트는 엔트리에서 구현할 수 없습니다.~~  
E106. 스크래치에서 형태 효과 중, "밝기", "투명도", "색"을 제외한 효과를 엔트리에서 구현할 수 없습니다.  
~~E107. 스크래치에서 신호를 보낼 때 계산한 이름으로 보내면, 엔트리에서 구현할 수 없습니다.~~  
E108. 스크래치에서 모양을 바꿀 때 계산한 이름으로 바꾸면, 엔트리에서 구현할 수 없습니다.  
E109. 스크래치에서 이벤트 구문 중, '아무'키를 눌렀을 때는 엔트리에서 구현할 수 없습니다.  
E110. 스크래치에서 소리 탭의 블럭은 엔트리에서 구현할 수 없습니다.  
E111. 스크래치에서 드래그 모드는 엔트리에서 구현할 수 없습니다.  
E112. 스크래치에서 마이크 음량 블럭은 엔트리에서 구현할 수 없습니다.  
E113. 스크래치에서 2000년이후 현재까지의 날짜 수 블럭은 엔트리에서 구현할 수 없습니다.  
E114. 스크래치에서 다른 오브젝트의 지역변수를 불러오는 블럭은 엔트리에서 구현할 수 없습니다.  
E115. 스크래치에서 특정 키를 감지하는 블럭에 계산한 이름을 넣으면, 엔트리에서 구현할 수 없습니다.  
E116. 스크래치에서 10^n, e^n을 엔트리에서 구현할 수 없습니다.  
E117. 스크래치에서 리스트 자체의 값(모든 항목을 한 문자열로 합침)을 엔트리에서 구현할 수 없습니다.  
E118. 스크래치에서 리스트에서 특정 항목의 위치를 계산하는 블럭을 엔트리에서 구현할 수 없습니다.  

## 변환은 가능하나, 의도와 다르게 동작할 수 있는 경우
E201. 벽에 닿았을 때 충돌 블럭은, 화면 사이즈가 서로 다르기 때문에 의도치 않게 동작할 수 있습니다.  
E202. 이미지 변환에 문제가 있어, 엔트리 파일을 연후 오브젝트>모양에서 저장하기를 직접 눌려주셔야 이미지가 제대로 표시됩니다.  
E203. 스크래치에서 앞, 뒤로 N칸 만큼 보내기 구문을 사용해도, 1까지만 엔트리에서 구현됩니다.  
E204. 스크래치와 엔트리는 크기 계산 방식이 달라, "크기 지정", "자신의 크기 값"을 사용 시에 문제가 생길 수 있습니다.  
E205. 스크래치에서 형태 구문 중, 다음 배경으로 바꾸기 후 배경이 바뀌었을 때 엔트리에서 이벤트가 작동하지 않습니다.  
E206. 스크래치에서 타이머는 실행하자마자 시작되지만, 엔트리에선 바로 시작되지 않습니다.  
E207. 스크래치와 엔트리는 요일 가져오기에서 베이스 (1~7, 0~6)이 다르므로, 에러가 발생할 수 있습니다.  
E208. 판단 값을 계산으로 사용할 때, 스크래치는 (true, false)형식, 엔트리는 (True, False)형식이므로 차이가 발생할 수 있습니다.  
E209. 스크래치와 엔트리에서 방향 처리 방식이 달라 다른 방향을 볼 수 있습니다.  
E210. 스크래치에서 회전 중심이 모양마다 다를 경우 엔트리에서 정확한 위치로 이동되지 않습니다.  